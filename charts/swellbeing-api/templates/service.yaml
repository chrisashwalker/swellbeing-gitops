apiVersion: v1
kind: Service
metadata:
  name: {{ include "swellbeing-api-chart.fullname" . }}
  labels:
    {{- include "swellbeing-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swellbeing-api-chart.selectorLabels" . | nindent 4 }}
